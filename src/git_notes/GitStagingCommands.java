package git_notes;

public class GitStagingCommands {
}

/*

public class GitStagingCommands {

    // Definition:
    // Used to move files into staging area before commit.

    // Add single file
    git add file.txt

    // Add all files
    git add .

    // Check git status
    git status

    // Unstage single file
    git restore --staged file.txt

    // Unstage all files
    git restore --staged .
}
 */
