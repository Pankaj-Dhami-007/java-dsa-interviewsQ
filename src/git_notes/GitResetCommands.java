package git_notes;

public class GitResetCommands {
}

/*

 // Definition:
    // Used to undo commits.

    // Undo commit but keep staged changes
    git reset --soft HEAD~1

    // Undo commit and unstage changes
    git reset HEAD~1

    // Undo commit and delete everything
    git reset --hard HEAD~1

    // Remove all local changes
    git reset --hard
 */
